<template v-if="item.isPathError">
    <a v-if="item.fileType==type.workDir" href="javascript:void(0)">
        <div class="board hover file-item" v-bind:class="{first: idx===0}">
            <div class="file-item-icon">
                <i class="fa-solid fa-folder-minus fa-3x fa-fw"></i>
            </div>
            <div class="file-item-text">
                <div v-if="!hasSearch" class="file-item-title">{{item.name}}</div>
                <div v-if="hasSearch" class="file-item-title" v-html="item.nameWithMark"></div>
                <div class="file-item-description">{{local.pathNotFound}}</div>
            </div>
        </div>
    </a>
</template>
<template v-if="!item.isPathError">
    <a v-if="item.fileType==type.workDir" :href="item.link" :target="linkTarget" :rel="linkRel" v-on:click="onWorkDirClick(item)">
        <div class="board hover file-item" v-bind:class="{active: selectedPath==getWorkDirPath(item), first: idx===0}">
            <div class="file-item-icon">
                <i class="fa-solid fa-folder-minus fa-3x fa-fw"></i>
            </div>
            <div class="file-item-text">
                <div v-if="!hasSearch" class="file-item-title">{{item.name}}</div>
                <div v-if="hasSearch" class="file-item-title" v-html="item.nameWithMark"></div>
                <div class="file-item-description">
                    <span>{{item.itemCount}} {{local.items}}</span>
                </div>
            </div>
        </div>
    </a>
</template>
<a v-if="item.fileType==type.folder" :href="item.link" :target="linkTarget" :rel="linkRel" v-on:click="onItemClick(item)">
    <div class="board hover file-item" v-bind:class="{active: selectedPath==getItemPath(item), first: idx===0}">
        <div class="file-item-icon">
            <i class="fa-solid fa-folder fa-3x fa-fw"></i>
        </div>
        <div class="file-item-text">
            <div v-if="!hasSearch" class="file-item-title">{{item.name}}</div>
            <div v-if="hasSearch" class="file-item-title" v-html="item.nameWithMark"></div>
            <div v-if="hasSearch" class="file-item-sub-title">
                <span class="file-item-sub-title-icon">
                    <i class="fa-solid fa-tag fa-fw"></i>
                </span>
                <span>{{item.searchDirPath}}</span>
            </div>
            <div class="file-item-description">
                <span>{{item.itemCount}} {{local.items}}</span>
            </div>
        </div>
    </div>
</a>
<a v-if="item.fileType!=type.folder" :href="routeLink('api/file/download', item.workNum, item.path)">
    <div v-if="item.fileType==type.video" class="board hover file-image-item" v-bind:class="{first: idx===0}">
        <div class="file-image-item-icon">
            <img :src="routeLink('api/thumbnail/video', item.workNum, item.path)" loading="lazy" />
        </div>
        <div class="file-image-item-text">
            <div v-if="!hasSearch" class="file-image-item-title">{{item.name}}</div>
            <div v-if="hasSearch" class="file-image-item-title" v-html="item.nameWithMark"></div>
            <div v-if="hasSearch" class="file-item-sub-title">
                <span class="file-item-sub-title-icon">
                    <i class="fa-solid fa-tag fa-fw"></i>
                </span>
                <span>{{item.searchDirPath}}</span>
            </div>
            <div class="file-image-item-description">{{item.fileSize}}</div>
        </div>
    </div>
    <div v-if="item.fileType==type.image" class="board hover file-image-item" v-bind:class="{first: idx===0}">
        <div class="file-image-item-icon">
            <img :src="routeLink('api/thumbnail/image', item.workNum, item.path)" loading="lazy" />
        </div>
        <div class="file-image-item-text">
            <div v-if="!hasSearch" class="file-image-item-title">{{item.name}}</div>
            <div v-if="hasSearch" class="file-image-item-title" v-html="item.nameWithMark"></div>
            <div v-if="hasSearch" class="file-image-item-sub-title">
                <span class="file-image-item-sub-title-icon">
                    <i class="fa-solid fa-tag fa-fw"></i>
                </span>
                <span>{{item.searchDirPath}}</span>
            </div>
            <div class="file-image-item-description">{{item.fileSize}}</div>
        </div>
    </div>
    <div v-if="item.fileType==type.audio" class="board hover file-item" v-bind:class="{first: idx===0}">
        <div class="file-item-icon">
            <i class="fa-solid fa-file-audio fa-3x fa-fw"></i>
        </div>
        <div class="file-item-text">
            <div v-if="!hasSearch" class="file-item-title">{{item.name}}</div>
            <div v-if="hasSearch" class="file-item-title" v-html="item.nameWithMark"></div>
            <div v-if="hasSearch" class="file-item-sub-title">
                <span class="file-item-sub-title-icon">
                    <i class="fa-solid fa-tag fa-fw"></i>
                </span>
                <span>{{item.searchDirPath}}</span>
            </div>
            <div class="file-item-description">{{item.fileSize}}</div>
        </div>
    </div>
    <div v-if="item.fileType==type.text" class="board hover file-item" v-bind:class="{first: idx===0}">
        <div class="file-item-icon">
            <i class="fa-solid fa-file-lines fa-3x fa-fw"></i>
        </div>
        <div class="file-item-text">
            <div v-if="!hasSearch" class="file-item-title">{{item.name}}</div>
            <div v-if="hasSearch" class="file-item-title" v-html="item.nameWithMark"></div>
            <div v-if="hasSearch" class="file-item-sub-title">
                <span class="file-item-sub-title-icon">
                    <i class="fa-solid fa-tag fa-fw"></i>
                </span>
                <span>{{item.searchDirPath}}</span>
            </div>
            <div class="file-item-description">{{item.fileSize}}</div>
        </div>
    </div>
    <div v-if="item.fileType==type.other" class="board hover file-item" v-bind:class="{first: idx===0}">
        <div class="file-item-icon">
            <i class="fa-solid fa-file fa-3x fa-fw"></i>
        </div>
        <div class="file-item-text">
            <div v-if="!hasSearch" class="file-item-title">{{item.name}}</div>
            <div v-if="hasSearch" class="file-item-title" v-html="item.nameWithMark"></div>
            <div v-if="hasSearch" class="file-item-sub-title">
                <span class="file-item-sub-title-icon">
                    <i class="fa-solid fa-tag fa-fw"></i>
                </span>
                <span>{{item.searchDirPath}}</span>
            </div>
            <div class="file-item-description">{{item.fileSize}}</div>
        </div>
    </div>
</a>